const mongoose = require('mongoose')
var shortHash = require('short-hash');

const ExamPapersSchema = new mongoose.Schema({
    
})

module.exports = mongoose.model('EXAMPAPERS',ExamPapersSchema,'EXAMPAPERS')
